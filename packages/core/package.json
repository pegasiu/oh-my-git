{
  "name": "@oh-my-git/core",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "dist",
    "src"
  ],
  "dependencies": {
    "@oh-my-git/shared": "workspace:*"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "format": "biome format . --write",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
